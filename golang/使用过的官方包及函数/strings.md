### 概述
+ strings包实现了用于操作字符的简单函数。

### 索引

+ `func HasPrefix(s, prefix string) bool`
+ `func HasSuffix(s, suffix string) bool`

### 说明

+ `func HasPrefix(s, prefix string) bool`
    + 判断s是否有前缀字符串prefix。
+ `func HasSuffix(s, suffix string) bool`
    + 判断s是否有后缀字符串suffix。